const state={rows:[],filtered:[],page:1,pageSize:50,sort:"sample",direction:1};const byId=(id)=>document.getElementById(id);
function parseCsv(text){const lines=text.trim().split(/\r?\n/);const columns=lines.shift().split(",");return lines.map((line)=>{const values=line.split(",");const row=Object.fromEntries(columns.map((column,index)=>[column,values[index]]));row.total_count=Number(row.total_count);row.count=Number(row.count);row.percentage=Number(row.percentage);return row})}
function applyFilters(){const query=byId("sample-search").value.trim().toLowerCase();const population=byId("population-filter").value;state.filtered=state.rows.filter((row)=>row.sample.toLowerCase().includes(query)&&(!population||row.population===population));state.filtered.sort((a,b)=>{const left=a[state.sort],right=b[state.sort];return(typeof left==="number"?left-right:left.localeCompare(right))*state.direction});state.page=1;renderTable()}
function renderTable(){const pages=Math.max(1,Math.ceil(state.filtered.length/state.pageSize));state.page=Math.min(state.page,pages);const start=(state.page-1)*state.pageSize;byId("frequency-body").innerHTML=state.filtered.slice(start,start+state.pageSize).map((row)=>`<tr><td>${row.sample}</td><td>${row.total_count.toLocaleString()}</td><td>${row.population}</td><td>${row.count.toLocaleString()}</td><td>${row.percentage.toFixed(2)}%</td></tr>`).join("");byId("page-summary").textContent=state.filtered.length?`${(start+1).toLocaleString()}–${Math.min(start+state.pageSize,state.filtered.length).toLocaleString()} of ${state.filtered.length.toLocaleString()}`:"No matching rows";byId("previous-page").disabled=state.page===1;byId("next-page").disabled=state.page===pages}
function closeMenu(){byId("sidebar").classList.remove("open");byId("scrim").classList.remove("open");byId("menu-button").setAttribute("aria-expanded","false")}
function showView(){const view=["part2","part3","part4"].includes(location.hash.slice(1))?location.hash.slice(1):"part2";document.querySelectorAll(".view").forEach((section)=>{section.hidden=section.id!==view});document.querySelectorAll("nav a").forEach((link)=>link.classList.toggle("active",link.dataset.view===view));closeMenu()}
async function loadData(){try{const response=await fetch("data/relative-frequency.csv");if(!response.ok)throw new Error(`HTTP ${response.status}`);state.rows=parseCsv(await response.text());state.filtered=[...state.rows];const populations=[...new Set(state.rows.map((row)=>row.population))].sort();populations.forEach((population)=>byId("population-filter").add(new Option(population,population)));byId("sample-count").textContent=new Set(state.rows.map((row)=>row.sample)).size.toLocaleString();byId("population-count").textContent=populations.length.toLocaleString();byId("row-count").textContent=state.rows.length.toLocaleString();byId("status").textContent="";applyFilters()}catch(error){byId("status").textContent="Could not load Part 2 data. Run `make pipeline`, then restart the dashboard."}}
byId("sample-search").addEventListener("input",applyFilters);byId("population-filter").addEventListener("change",applyFilters);byId("page-size").addEventListener("change",(event)=>{state.pageSize=Number(event.target.value);state.page=1;renderTable()});byId("previous-page").addEventListener("click",()=>{state.page--;renderTable()});byId("next-page").addEventListener("click",()=>{state.page++;renderTable()});document.querySelectorAll("th button").forEach((button)=>button.addEventListener("click",()=>{state.direction=state.sort===button.dataset.sort?-state.direction:1;state.sort=button.dataset.sort;applyFilters()}));byId("menu-button").addEventListener("click",()=>{const open=byId("sidebar").classList.toggle("open");byId("scrim").classList.toggle("open",open);byId("menu-button").setAttribute("aria-expanded",String(open))});byId("scrim").addEventListener("click",closeMenu);window.addEventListener("hashchange",showView);showView();loadData();
